# Cycle.js Exploration

## Usage

1.  Open the directory of an example in your terminal.
2.  Run `npm run clean`
3.  Run `npm start` (live-edit)

A browser should be opened at: `http://localhost:3000/`
